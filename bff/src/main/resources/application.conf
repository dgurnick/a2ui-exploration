ktor {
    deployment {
        port = 8080
        host = "0.0.0.0"
        watch = [ "classes", "resources" ]
    }
    application {
        modules = [ com.dgurnick.banking.bff.ApplicationKt.module ]
    }
}
